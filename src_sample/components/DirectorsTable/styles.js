export const styles = (theme) => ({
  searchRoot: {
    marginBottom: theme.spacing(3),
    minHeight: theme.spacing(9),
    display: 'flex',
  },
  root: {
    width: '100%',
    overflowX: 'auto',
  },
});
